# SPDX-FileCopyrightText: 2010-2026 the Friendica project
#
# SPDX-License-Identifier: CC0-1.0

name: Code Quality

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:

  code-style:
    name: PHP-CS-Fixer (PHP ${{ matrix.php }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        operating-system: ['ubuntu-latest']
        php: ['8.5']

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - name: Setup PHP with composer and extensions
        uses: shivammathur/setup-php@v2 #https://github.com/shivammathur/setup-php
        with:
          php-version: ${{ matrix.php }}
          coverage: xdebug
          tools: none

      - name: Clone addon repository
        run: |
          git clone -b develop --single-branch https://github.com/friendica/friendica-addons.git /tmp/friendica-addons
          cp -rn /tmp/friendica-addons/. addon/

      - name: Install Composer dependencies
        uses: "ramsey/composer-install@v4"

      - name: Run PHP-CS-Fixer
        continue-on-error: true
        run: vendor/bin/php-cs-fixer fix --diff --dry-run

  phpstan:
    name: PHPStan (PHP ${{ matrix.php }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        operating-system: ['ubuntu-latest']
        php: ['8.5']

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - name: Setup PHP with composer and extensions
        uses: shivammathur/setup-php@v2 #https://github.com/shivammathur/setup-php
        with:
          php-version: ${{ matrix.php }}
          coverage: xdebug
          tools: none

      - name: Clone addon repository
        run: |
          git clone -b develop --single-branch https://github.com/friendica/friendica-addons.git /tmp/friendica-addons
          cp -rn /tmp/friendica-addons/. addon/

      - name: Install Composer dependencies
        uses: "ramsey/composer-install@v4"

      - name: Run PHPStan
        run: composer run phpstan

  phpstan-addons:
    name: PHPStan in addons (PHP ${{ matrix.php }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        operating-system: ['ubuntu-latest']
        php: ['8.5']

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - name: Setup PHP with composer and extensions
        uses: shivammathur/setup-php@v2 #https://github.com/shivammathur/setup-php
        with:
          php-version: ${{ matrix.php }}
          coverage: xdebug
          tools: none

      - name: Clone addon repository
        run: |
          git clone -b develop --single-branch https://github.com/friendica/friendica-addons.git /tmp/friendica-addons
          cp -rn /tmp/friendica-addons/. addon/

      - name: Install Composer dependencies
        uses: "ramsey/composer-install@v4"

      # Non-blocking: friendica-addons lives on git.friendi.ca and has no CI of its own,
      # so faulty addon code only surfaces here, after it was merged - turning this
      # repository's pipeline red for changes that aren't ours. The findings stay
      # visible in the log, the annotations and the job summary below.
      - name: Run PHPStan in addons
        id: phpstan_addons
        continue-on-error: true
        run: 'composer run addons:phpstan'

      - name: Report addon PHPStan findings
        if: steps.phpstan_addons.outcome == 'failure'
        run: |
          {
            echo '### :warning: PHPStan found errors in friendica-addons'
            echo
            echo 'These come from the addon repository on git.friendi.ca, not from this pull request,'
            echo 'so they do not fail this workflow. See the "Run PHPStan in addons" step for details.'
          } >> "$GITHUB_STEP_SUMMARY"

  rector:
    name: Rector (PHP ${{ matrix.php }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        operating-system: ['ubuntu-latest']
        php: ['8.5']

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - name: Setup PHP with composer and extensions
        uses: shivammathur/setup-php@v2 #https://github.com/shivammathur/setup-php
        with:
          php-version: ${{ matrix.php }}
          tools: none

      - name: Clone addon repository
        run: |
          git clone -b develop --single-branch https://github.com/friendica/friendica-addons.git /tmp/friendica-addons
          cp -rn /tmp/friendica-addons/. addon/

      - name: Install Composer dependencies
        uses: "ramsey/composer-install@v4"

      - name: Run Rector
        run: composer run rector:check

  phpmd:
    name: PHPMD (PHP ${{ matrix.php }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        operating-system: ['ubuntu-latest']
        php: ['8.5']

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - name: Setup PHP with composer and extensions
        uses: shivammathur/setup-php@v2 #https://github.com/shivammathur/setup-php
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          tools: none

      - name: Clone addon repository
        run: |
          git clone -b develop --single-branch https://github.com/friendica/friendica-addons.git /tmp/friendica-addons
          cp -rn /tmp/friendica-addons/. addon/

      - name: Install Composer dependencies
        uses: "ramsey/composer-install@v4"

      - name: Run PHPMD
        run: vendor/bin/phpmd src/ text .phpmd-ruleset.xml --color

  eslint:
    name: ESLint (Node ${{ matrix.node }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        operating-system: ['ubuntu-latest']
        node: ['24']

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - name: Install npm dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint:js

  stylelint:
    name: Stylelint (Node ${{ matrix.node }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        operating-system: ['ubuntu-latest']
        node: ['24']

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - name: Install npm dependencies
        run: npm ci

      - name: Run Stylelint
        run: npm run lint:css
