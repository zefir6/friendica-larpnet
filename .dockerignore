scripts/
.git/
.github/
.docker/
.claude/
.DS_Store
.env
# Local dev secrets (DB credentials, site keys) - must never be baked into the image.
config/local.config.php
# Built fresh by the Dockerfile via composer install; never trust a local copy.
vendor/
# Same idea for the larpnet_matrix chat client -- built fresh by the
# matrix-client-builder stage via `npm ci`; never trust a local copy.
addon/larpnet_matrix/client/node_modules/
addon/larpnet_matrix/client/dist/
