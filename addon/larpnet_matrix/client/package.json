{
  "name": "larpnet-matrix-client",
  "private": true,
  "version": "1.0.0",
  "description": "Larpnet's own minimal Matrix web client (1:1 DMs + room list), served same-origin from the larpnet_matrix Friendica addon. Replaces the earlier vectorim/element-web + separate chat subdomain approach.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "matrix-js-sdk": "42.4.0",
    "matrix-encrypt-attachment": "1.0.3",
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
