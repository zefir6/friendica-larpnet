# SPDX-FileCopyrightText: 2010-2026 the Friendica project
#
# SPDX-License-Identifier: CC0-1.0

matrix:
  include:
    - PHP_MAJOR_VERSION: 8.5
      PHP_VERSION: 8.5.5

# The phpmd check is just triggered for PRs and pushes to non-stable branches of Friendica
when:
  branch:
    exclude: [ stable ]
  event: [ pull_request, push ]

# This forces PHP Unit executions at the "opensocial" labeled location (because of access issues with git.friendi.ca)
labels:
  location: opensocial

steps:
  restore_cache:
    image: meltwater/drone-cache:dev
    settings:
      backend: "filesystem"
      restore: true
      cache_key: "{{ .Repo.Name }}_php${PHP_MAJOR_VERSION}_{{ arch }}_{{ os }}"
      archive_format: "gzip"
      mount:
        - '.composer'
    volumes:
      - /tmp/drone-cache:/tmp/cache

  composer_install:
    image: friendicaci/php${PHP_MAJOR_VERSION}:php${PHP_VERSION}
    commands:
      - git clone -b develop --single-branch https://git.friendi.ca/friendica/friendica-addons.git addon
      - export COMPOSER_HOME=.composer
      - ./bin/composer.phar validate
      - ./bin/composer.phar install --prefer-dist

  rebuild_cache:
    image: meltwater/drone-cache:dev
    settings:
      backend: "filesystem"
      rebuild: true
      cache_key: "{{ .Repo.Name }}_php${PHP_MAJOR_VERSION}_{{ arch }}_{{ os }}"
      archive_format: "gzip"
      mount:
        - '.composer'
    volumes:
      - /tmp/drone-cache:/tmp/cache

  phpstan:
    image: friendicaci/php${PHP_MAJOR_VERSION}:php${PHP_VERSION}
    commands:
      - bin/composer.phar run phpstan;

  # Non-blocking: friendica-addons lives on git.friendi.ca and has no CI of its own,
  # so faulty addon code only surfaces here, after it was merged - turning this
  # repository's pipeline red for changes that aren't ours.
  phpstan-addons:
    image: friendicaci/php${PHP_MAJOR_VERSION}:php${PHP_VERSION}
    failure: ignore
    commands:
      - bin/composer.phar run addons:phpstan

  rector:
    image: friendicaci/php${PHP_MAJOR_VERSION}:php${PHP_VERSION}
    commands:
      - bin/composer.phar run rector:check;

  phpmd:
    image: friendicaci/php${PHP_MAJOR_VERSION}:php${PHP_VERSION}
    commands:
      - ./bin/composer.phar run phpmd
